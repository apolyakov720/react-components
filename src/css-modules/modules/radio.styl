.radio
  @extend .switcher

  &__input
    border-radius 50%

    &::before
      content ''
      position absolute
      top 0
      right 0
      bottom 0
      left 0
      background transparent
      border-radius inherit
      transform scale(.7)
    
    ~/ input:checked + &::before
      background $blue-violet